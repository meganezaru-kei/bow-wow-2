.card
  .card-header
    %p.show-post__info
      %span
        = icon("far", "edit")
        = post.created_at.to_s(:datetime_day_jp)
      &nbsp
      %span
        = icon("fas", "user")
        = post.user.name + "さん"
    %h4
      = post.title
    - post.tags.each do |tag|
      %span.badge.badge-info
        = tag.name
  .card-body
    %ul.slider
      - if post.images.attached?
        - post.images.each do |image|
          %li
            = image_tag image, class: 'show-post__images'
  .card-body
    %p.card-text
      = simple_format(post.body)
  